# Integrations test in across containers

Run the basic integrations test across containers, using volumes-from and links.

See `run.sh` for details

Just `./build.sh` to build and run the whole thing
